from app.db import get_context_chain
from app.model_client import call_model
from app.retrieval import find_similar_messages
from app.settings import MIN_NICHE_SCORE, MIN_INTENT_SCORE

PROMPT_VERSION = "classifier_v7_strict_niche_reply_author"


def format_list(items) -> str:
    if not items:
        return "Не указаны"

    if isinstance(items, str):
        return items.strip() or "Не указаны"

    return "\n".join(
        f"- {item}"
        for item in items
        if item
    )


def build_prompt(
    text: str,
    niche: dict,
    reply_text: str | None = None,
    reply_author_relation: str | None = None,
) -> str:
    company_name = niche.get("company_name") or "Не указана"
    niche_name = niche.get("name") or "Не указана"
    about = niche.get("about") or "Не указано"
    keywords = format_list(niche.get("keywords") or [])
    blacklist = format_list(niche.get("blacklist") or [])

    if reply_text:
        reply_block = f"""
Автор reply относительно автора текущего сообщения:
{reply_author_relation}

Текст reply:
{reply_text}
    """.strip()
    else:
        reply_block = "Reply отсутствует."


    return f"""
РОЛЬ:

Ты анализируешь одно сообщение из общего потока разных Telegram-чатов.

Сообщение может относиться к любой теме.
Название компании, описание ниши и ключевые слова не означают,
что сообщение связано с этой нишей.

Независимо оцени два показателя:

1. niche_score
2. intent_score


ТЕКУЩАЯ НИША:

Компания:
{company_name}

Название:
{niche_name}

Описание услуг:
{about}

Тематические подсказки:
{keywords}

Исключённые направления:
{blacklist}


NICHE_SCORE:

niche_score показывает, насколько тема или задача сообщения
относится к услугам текущей ниши.

Используй только текущее сообщение и reply, если он передан.

Шкала:

0-20 — другая тема или задача не указана.
21-49 — возможна слабая связь, которую приходится додумывать.
50-74 — сообщение тематически близко, но задача описана неясно.
75-89 — тема или задача явно относится к текущей нише.
90-100 — задача напрямую соответствует конкретным услугам текущей ниши.

Правила:

- описание ниши используется только для сравнения;
- описание ниши не является контекстом сообщения;
- ключевые слова являются только тематическими подсказками;
- нельзя переносить информацию из описания ниши в сообщение;
- нельзя додумывать отсутствующую тему или задачу;
- просьба о помощи не повышает niche_score без указания темы;
- упоминание оплаты не повышает niche_score;
- поиск исполнителя не повышает niche_score, если задача неизвестна;
- сообщение, подходящее к любой профессии или услуге, должно получить 0-30;
- запрос по другой нише должен получить низкий niche_score;
- направление из списка исключений должно получить низкий niche_score;
- reply может использоваться для восстановления темы текущего сообщения;
- принадлежность reply другому автору не запрещает использовать его
  для определения темы, но не позволяет переносить намерение его автора;
- niche_score 75 и выше допустим только тогда, когда в текущем сообщении
  или reply есть конкретная тема, объект, процесс или проблема,
  которые можно напрямую сопоставить с услугами ниши;
- если связь с нишей строится только на общих словах, тематических подсказках,
  предполагаемом контексте чата или описании услуг, niche_score должен быть
не выше 49;
- если сообщение можно полностью понять без обращения к услугам текущей ниши,
  нельзя додумывать связь с ней.


INTENT_SCORE:

intent_score показывает, насколько автор текущего сообщения
сам нуждается в помощи, консультации, обучении, сопровождении
или выполнении задачи.

Оценивай намерение только автора текущего сообщения.

Сначала определи роль текущего сообщения:

- автор задаёт вопрос, просит помощь или описывает нерешённую задачу;
- автор отвечает другому человеку и даёт решение;
- автор обсуждает или комментирует чужую ситуацию;
- автор предлагает собственные услуги;
- автор сообщает информацию без запроса.

Высокий intent_score допустим только тогда, когда сам автор
текущего сообщения выражает собственную потребность в помощи
или консультации.


СВЯЗЬ С REPLY:

Информация о том, тот же или другой автор написал reply,
является достоверной. Не пытайся определять это самостоятельно по тексту.

Если reply написал тот же автор:

- reply может содержать предыдущую часть собственной задачи автора;
- оценивай текущее сообщение вместе с reply как продолжение одной мысли;
- короткое уточнение, вопрос о стоимости, порядке действий, сроках,
  последствиях или инструкции может получить высокий intent_score;
- благодарность или подтверждение не снижают intent_score,
  если после них автор задаёт новый вопрос;
- описание собственной нерешённой проблемы в reply можно учитывать
  при оценке намерения текущего автора.

Если reply написал другой автор:

- reply используется только для понимания темы разговора;
- нельзя переносить вопрос, проблему, намерение или потребность
  автора reply на автора текущего сообщения;
- ответ, совет, инструкция, готовое решение или разъяснение
  должны получить intent_score не выше 20;
- короткая реакция, удивление, уточнение деталей или обсуждение
  чужой ситуации сами по себе не являются потребностью в услуге;
- вопрос о том, что произошло у другого участника,
  не означает собственную потребность автора;
- высокий intent_score возможен только тогда, когда текущий автор
  явно описывает свою аналогичную задачу, просит решение для себя,
  ищет исполнителя или запрашивает консультацию для собственного случая.

Признаки ответа или консультации другому человеку:

- автор объясняет, как следует поступить;
- рекомендует конкретные действия;
- сообщает правильный ответ или правило;
- подтверждает или опровергает информацию;
- предлагает попробовать определённое решение;
- описывает, что должен сделать собеседник.

Такие сообщения должны получить intent_score не выше 20,
даже если reply содержит явную проблему потенциального клиента.


Шкала:

0-20 — автор не ищет помощь: отвечает, консультирует, даёт инструкцию,
комментирует чужую ситуацию, предлагает свои услуги, сообщает информацию
или описывает уже решённую ситуацию.

21-49 — обсуждение, реакция или неясный интерес без собственной задачи
и без явного запроса на консультацию.

50-74 — возможна собственная задача или потребность,
но намерение автора выражено неясно.

75-89 — автор задаёт конкретный вопрос по собственной ситуации,
просит совет или описывает свою нерешённую проблему.

90-100 — автор прямо ищет помощь, специалиста, обучение,
сопровождение, стоимость услуги или выполнение работы.

Правила:

- конкретный вопрос по теме ниши может получить высокий intent_score;
- вопрос о выборе способа, порядке действий, правилах, сроках,
  последствиях, инструкции или значении термина
  может быть запросом на консультацию;
- уточняющий вопрос может получить высокий intent_score,
  если относится к собственной задаче автора;
- автору необязательно прямо искать платную услугу или исполнителя;
- описание ошибки или собственной проблемы может получить высокий intent_score;
- вопрос о стоимости является признаком явного намерения;
- предложение собственных услуг не является потребностью автора;
- поиск сотрудника в штат не является запросом услуги;
- новости и объявления без вопроса или нерешённой задачи
  должны получить низкий intent_score;
- наличие темы ниши само по себе не повышает intent_score;
- нельзя считать любое сообщение с вопросительным знаком
  запросом на услугу;
- вопрос о чужой ситуации без собственной задачи должен получить
  intent_score не выше 49;
- ответ, рекомендация, инструкция, разъяснение или готовое решение
  должны получить intent_score не выше 20.


REPLY:

{reply_block}


ТЕКУЩЕЕ СООБЩЕНИЕ:

{text}


ОТВЕТ:

Верни только валидный JSON без markdown и пояснений:

{{
  "niche_score": 0,
  "intent_score": 0,
  "description": "краткое объяснение оценок"
}}

Требования:

- niche_score — целое число от 0 до 100;
- intent_score — целое число от 0 до 100;
- description — одно короткое предложение на русском языке;
- description объясняет связь с нишей и намерение именно автора
  текущего сообщения;
- не возвращай поле lead;
- не добавляй текст до или после JSON;
- description не должно утверждать наличие темы или задачи,
  которых нет в текущем сообщении или reply.
""".strip()


def build_memory_examples(text: str, niche_id: int):
    rows = find_similar_messages(text, niche_id, 5)

    if not rows:
        return "Пока нет похожих примеров."

    examples = []

    for row in rows:
        ai_lead = "true" if row["ai_lead"] else "false"
        human_lead = "true" if row["human_lead"] else "false"

        chain = get_context_chain(
            tg_chat_id=row.get("tg_chat_id"),
            tg_message_id=row.get("tg_message_id"),
            reply_to_id=row.get("reply_to_id"),
        )

        if not chain:
            continue

        example = []

        if len(chain) > 1:
            example.append("Контекст диалога:")

            for msg in chain[:-1]:
                example.append(f"- {msg}")

        example.append(f'Сообщение: "{chain[-1]}"')
        example.append(
            f"Результат: ai_lead={ai_lead}, human_lead={human_lead}"
        )
        examples.append("\n".join(example))

    return "\n\n".join(examples)


def normalize_ai_score(value) -> int:
    try:
        score = int(value)
    except (TypeError, ValueError):
        return 0

    return max(0, min(score, 100))


def is_lead(
    text: str,
    niche: dict,
    sender_id: int | None = None,
    reply_text: str | None = None,
    reply_sender_id: int | None = None,
) -> dict | None:
    if not reply_text:
        reply_author_relation = "reply отсутствует"
    elif sender_id is None or reply_sender_id is None:
        reply_author_relation = "неизвестно"
    elif sender_id == reply_sender_id:
        reply_author_relation = "тот же автор"
    else:
        reply_author_relation = "другой автор"

    prompt = build_prompt(
        text=text,
        niche=niche,
        reply_text=reply_text,
        reply_author_relation=reply_author_relation,
    )

    data = call_model(prompt)

    if not data:
        print("❌ is_lead: call_model returned None", flush=True)
        print(f"TEXT: {text}", flush=True)
        print(f"NICHE: {niche.get('name')}", flush=True)
        return None

    niche_score = normalize_ai_score(
        data.get("niche_score")
    )

    intent_score = normalize_ai_score(
        data.get("intent_score")
    )

    final_lead = (
        niche_score >= MIN_NICHE_SCORE
        and intent_score >= MIN_INTENT_SCORE
    )

    return {
        "lead": final_lead,
        "niche_score": niche_score,
        "intent_score": intent_score,
        "description": data.get("description") or "",
        "reply_author_relation": reply_author_relation,
        "raw_response": data,
        "prompt": prompt,
        "prompt_version": PROMPT_VERSION,
    }
